import React from 'react';
import PokemonCharactersContainer from './PokemonCharactersContainer';

// this is just the shell container so I can store the PokemonCharactersContainer component which has got the main stuff
function PokedexContainer() {
    return (
        <PokemonCharactersContainer />
    );
}

export default PokedexContainer;